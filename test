fun foo(idx:number,accumulator:number)-> number
do
    if idx == 10 then
       return accumulator
    end
    var incremented = idx + 1
    return foo(incremented ,accumulator+incremented)

end

fun bar()->number
do
    return foo(0,0)
end
