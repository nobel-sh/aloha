pub extern type Arena;

extern fun aloha_arena_new() -> &Arena;
extern fun aloha_arena_free_all(arena: &Arena) -> void;

pub fun arena_new() -> &Arena {
    return aloha_arena_new();
}

pub fun arena_free_all(arena: &Arena) -> void {
    aloha_arena_free_all(arena);
}
