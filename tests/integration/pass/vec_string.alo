fun main() -> void {
    imut arena = arena_new();
    imut names = vec_string_new(arena);

    vec_string_push(names, "a");
    vec_string_push(names, "bb");
    vec_string_push(names, "ccc");
    vec_string_push(names, "dddd");
    vec_string_push(names, "eeeee");
    vec_string_push(names, "ffffff");
    vec_string_push(names, "ggggggg");
    vec_string_push(names, "hhhhhhhh");
    vec_string_push(names, "iiiiiiiii");
    vec_string_push(names, "jjjjjjjjjj");

    assert_msg(vec_string_len(names) == 10, "vec string len works");
    assert_msg(vec_string_get(names, 1) == "bb", "vec string get works");
    assert_msg(vec_string_get(names, 9) == "jjjjjjjjjj", "vec string growth preserves values");

    vec_string_set(names, 5, "updated");
    assert_msg(vec_string_get(names, 5) == "updated", "vec string set works");
    assert_msg(vec_string_index_of(names, "updated") == 5, "vec string index_of finds updated value");
    assert_msg(vec_string_index_of(names, "missing") == -1, "vec string index_of missing returns -1");
    assert_msg(vec_string_contains(names, "bb"), "vec string contains finds existing value");
    assert_msg(vec_string_contains(names, "missing") == false, "vec string contains rejects missing value");

    arena_free_all(arena);
}
