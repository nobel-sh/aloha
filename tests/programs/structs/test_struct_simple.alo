import "stdlib/io.alo";

struct Point {
  x: number,
  y: number
}

fun main() -> number {
    imut point = Point{10, 20};

    imut x_val = point->x;
    imut y_val = point->y;

    printlnNum(x_val);
    printlnNum(y_val);

    return 0;
}
