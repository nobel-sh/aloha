import "stdlib/io.alo";

fun main() -> number {
    // Test type inference
    imut x = 42;          // should infer int;
    imut y = 3.14;        // should infer float;
    imut z = x + 10;      // should infer int;
 
    printlnNum(z);
    println("No errors. Type inference works!");
    return 0;
}
