fun add_to_n(num: number, acc: number) -> number
do
    if num == 0 then
       return acc
    end
    return add_to_n(num - 1, acc + 1)
end

fun main()->void
do
    var k = add_to_n(10, 0)
    printlnNum(k)
end
