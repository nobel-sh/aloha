fun add_to_n(num: number, acc: number) -> number {
    if num == 0 {
       return acc
    }
    return add_to_n(num - 1, acc + 1)
}

fun main() -> void {
    imut k = add_to_n(10, 0)
    printlnNum(k)
}
